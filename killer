echo "started kill script"
while true
    do
        kill $( ps aux | grep 'gzserver' | awk '{print $2}');
        kill $( ps aux | grep 'revolve.py' | awk '{print $2}' );
        sleep 900s
        echo "killed once again"
    done
